version: '3.8'
services:
  client:
    build: ./client
    ports:
      - 5173:5173
    environment:
      - VITE_API_URL=http://host.docker.internal:5001/api
  server:
    build: ./server
    ports:
      - 5001:5001
    volumes:
      - ./server:/app
